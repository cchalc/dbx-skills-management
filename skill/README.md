# skill/

Home of the **`databricks-blog-forge`** skill (working name): research → draft content →
configure a FEVM workspace scratch area for testing blog material → publish to prose.sh.

Follows `ai-kitchen-dbx` skill conventions (kebab `name`, "when to use" + negative triggers,
`compatible-agents`). `SKILL.md` lands here in Phase 3. Dual-distributable via the Vibe
marketplace and/or the workspace/UC reconcile DAB, but defaults to local use first.
